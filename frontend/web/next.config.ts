import type { NextConfig } from "next"; // Next.js config typing

const nextConfig: NextConfig = {
  /* config options here */ // Keep default settings unless features require overrides
};

export default nextConfig; // Export Next.js configuration
